{
  "buildCommand": "npx prisma generate && npm run build --workspace=packages/shared-types && npm run build --workspace=apps/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "npm install",
  "framework": "nextjs"
}
