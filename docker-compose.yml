services:
  postgres:
    image: postgres:15-alpine
    container_name: alip-postgres
    environment:
      POSTGRES_USER: alip
      POSTGRES_PASSWORD: alip_secret
      POSTGRES_DB: alip
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U alip -d alip"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  postgres_data:
    driver: local
